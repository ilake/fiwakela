<%= render :partial => 'index/top_menu', :locals => {:current_facebook_user => @current_facebook_user, :user => @user} %>
<% fb_serverfbml do %>
  <script type="text/fbml">
    <fb:fbml>

    <fb:request-form
    action="<%= friend_url(:path => false) %>"
    method="GET"
    invite="true"
    type="<%= t('common.apname')%>"
    content=" <fb:req-choice url='<%= FB_APP_URL %>' label='確認' /> " >
    <fb:multi-friend-selector showborder="true" 
    max="10" 
    exclude_ids='<%= @e_ids %>'
    actiontext="<%= t('friend.invite')%>" />
    </fb:request-form>
    </fb:fbml>
  </script>
<% end %>

